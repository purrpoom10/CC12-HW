// const names = ['Mike', 'Sid', 'Jack', 'Bill'];

// function readAll(item,index,array){
// console.log(`${index+1}. ${item}`)

// }

// names.forEach(readAll);

const names = ['Mike', 'Sid', 'Jack', 'Bill'];

names.forEach(function (item, index) {
  console.log(`${index + 1}. ${item}`);
});
